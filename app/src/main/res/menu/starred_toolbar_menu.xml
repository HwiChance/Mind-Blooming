<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/starredSearchMenu"
        android:contentDescription="@string/search_menu_description"
        android:icon="@drawable/ic_search_24dp"
        android:title="@string/search_menu"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView" />

    <item
        android:id="@+id/starredFilterMenu"
        android:contentDescription="@string/filter_menu_description"
        android:icon="@drawable/ic_filter_list_24dp"
        android:title="@string/filter_menu"
        app:showAsAction="ifRoom" />
</menu>